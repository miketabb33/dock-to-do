import React from 'react'
import styled from 'styled-components'

const StyledTitle = styled.h1`
  text-align: center;
  font-size: 6rem;
`

const Title = () => {
  return <StyledTitle>Dock To Do</StyledTitle>
}

export default Title
